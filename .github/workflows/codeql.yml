name: merge pull request
on:
  pull_request:
    types: [closed]
    branches:
      - multi env

jobs:
  name: merge pull request 
  runs-on: ubuntu-latest
   steps:
     - name: checkout repository
      uses: actions/checkout@v2

    
    
    
